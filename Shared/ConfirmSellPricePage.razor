@using Syncfusion.Blazor.Popups

<SfDialog @ref="DialogSellPrice" @bind-Visible="@IsVisible"
          AllowDragging="true" IsModal="true"
          Width="500px" ShowCloseIcon="true">
    <DialogTemplates>
        <Header> @SellPriceHeaderMessage </Header>
        <Content>@SellPriceContentMessage</Content>
    </DialogTemplates>
    <DialogButtons>
        <DialogButton Content="Confirm" IsPrimary="true" OnClick="() => OnConfirmationChange(true)" />
        <DialogButton Content="Cancel" IsPrimary="false" OnClick="() => OnConfirmationChange(false)" />
    </DialogButtons>
</SfDialog>

@code {
    SfDialog? DialogSellPrice;
    public bool IsVisible { get; set; } = false;

    [Parameter] public string? SellPriceHeaderMessage { get; set; }
    [Parameter] public string? SellPriceContentMessage { get; set; }

    [Parameter] public EventCallback<bool> ConfirmationChanged { get; set; }

    public void OpenDialog()
    {
        this.IsVisible = true;
        this.StateHasChanged();
    }

    protected async Task OnConfirmationChange(bool value)
    {
        this.IsVisible = false;
        await ConfirmationChanged.InvokeAsync(value);
    }

}